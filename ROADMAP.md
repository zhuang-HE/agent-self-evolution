# 🗺️ 进化路线图

> 最后更新：2026-04-24

---

## ✅ 已完成（v1.0）

### 核心框架搭建
- [x] 设计三层 Skill 闭环架构（self-improvement → memory-consolidation → skill-evolution）
- [x] self-improvement v2.1：自动触发强化 + Skill 候选识别 + 跨会话模式检测
- [x] memory-consolidation v2.1：关键词预检 + 记忆摘要压缩
- [x] skill-evolution v1.2：踩坑经验即时追加 + 经验自动消化

### 记忆系统点火
- [x] MEMORY.md 结构化模板（项目分区 + 置信度标记）
- [x] 日期日志模板
- [x] 从 SOUL.md / USER.md / 系统记忆中提取初始数据

### Hermes 借鉴
- [x] Hermes Agent 记忆系统分析
- [x] Hermes Agent Skill 自动创建机制分析
- [x] Hermes Agent 自进化闭环分析
- [x] 借鉴分析报告输出

---

## 📋 下一步计划

### 高优先级

#### 1. Skill 自动创建 MVP
**目标**：完成复杂任务后，自动生成 SKILL.md 模板供用户审核

**实现路径**：
```
任务完成（≥ 3 个可复用步骤）
  → self-improvement 识别为 Skill 候选
  → 提取：触发词 + 工作流 + 核心规则
  → 生成 SKILL.md 模板
  → 提交用户审核
  → 审核通过 → 写入 ~/.workbuddy/skills/
```

**验收标准**：
- [ ] 能从 5 次以上的任务执行中提取可复用模式
- [ ] 生成的 SKILL.md 符合 agentskills.io 基本格式
- [ ] 用户审核流程清晰（展示模板 → 确认/修改/拒绝）

### 中优先级

#### 2. 记忆索引结构化
**目标**：MEMORY.md 超过 50KB 时，引入 JSON 索引实现按需检索

**实现路径**：
```
MEMORY.md 内容 → 生成 memory-index.json
  → 索引结构：{ keyword, section, confidence, last_referenced }
  → 会话开始时：读取关键词 → 匹配当前任务 → 只加载相关分区
  → 预期效果：token 消耗降低 60%+
```

#### 3. 踩坑经验自动消化
**目标**：当同一 Skill 踩坑 ≥ 3 条时，自动提炼为工作流显式步骤

**实现路径**：
```
skill-evolution 审计时
  → 统计每个 Skill 的踩坑经验数
  → ≥ 3 条 → 分析共同模式
  → 提炼为 SKILL.md 工作流中的显式步骤
  → 标记已消化的经验为 [已整合]
```

### 低优先级

#### 4. 跨会话模式检测
**目标**：每次会话开始时，自动检测与历史记忆中的重复任务模式

**实现路径**：
```
会话开始 → 读取 MEMORY.md
  → 提取任务关键词
  → 与历史日志对比
  → 发现重复模式 → 建议使用已有 Skill 或工作流
```

---

## ⏸️ 暂不适用

| 功能 | 原因 | 前提条件 |
|------|------|---------|
| RL 数据飞轮 | WorkBuddy 是云端产品，无法微调底层模型 | 支持自定义模型 |
| 提示词自动优化（DSPy + GEPA） | 需要 API 成本预算 | 确定预算方案 |
| FTS5 全文索引 | 需要 SQLite 数据库层 | 记忆系统迁移到数据库 |

---

## 📅 里程碑

| 里程碑 | 预期时间 | 核心交付 |
|--------|---------|---------|
| v1.0 基础闭环 | 2026-04-24 ✅ | 三个核心 Skill + 记忆模板 |
| v1.1 Skill 自动创建 | TBD | MVP 自动生成 SKILL.md |
| v2.0 智能记忆 | TBD | JSON 索引 + 按需检索 |
| v2.1 经验消化 | TBD | 踩坑 → 显式步骤自动化 |
| v3.0 完全自进化 | TBD | 跨会话模式 + 提示词优化 |
